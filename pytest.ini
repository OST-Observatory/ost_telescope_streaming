[pytest]
testpaths = tests
pythonpath = code
addopts = -q -m "not integration" -p no:debugging
markers =
    integration: marks tests that require integrated systems or heavy IO
    hardware: marks tests that need real hardware connected
    slow: marks tests as slow
filterwarnings =
    ignore:.*could not be resolved:UserWarning
